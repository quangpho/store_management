@model IEnumerable<StoreManagement.Entity.Dress>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Dress List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Drescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OriginialCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoughtDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Drescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OriginialCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoughtDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DressId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DressId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DressId })
        </td>
    </tr>
}

</table>
